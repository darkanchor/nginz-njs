{
  "name": "nginz-njs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "format":       "bun scripts/format.js",
    "update":       "bun scripts/update.js",
    "update:module": "bun scripts/update.js",
    "build":        "bun scripts/build.js",
    "build:module": "bun scripts/build.js",
    "test:unit":    "bun scripts/test.js",
    "test:int":     "bun scripts/build.js && find modules -path '*/tests/basic/do.test.js' | xargs bun test",
    "test:native":  "bun scripts/build.js && find modules -path '*/tests/*/do.test.js' | xargs bun test",
    "test":         "bun scripts/test.js && bun scripts/build.js && find modules -path '*/tests/*/do.test.js' | xargs bun test",
    "deploy":       "bun scripts/deploy.js",
    "clean":        "rm -rf dist modules/*/build modules/*/manifest.toml",
    "gh-issue":    "bun scripts/gh-issue.js"
  },
  "devDependencies": {}
}
